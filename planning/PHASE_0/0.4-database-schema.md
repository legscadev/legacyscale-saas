# Task 0.4 — Design Full Database Schema

## Overview

Create the complete Prisma schema for all MVP modules: users, courses, chapters, lessons, quizzes, progress tracking, notes, and announcements.

**Estimated Time:** 4 hours
**Priority:** P1 (Blocker)
**Dependencies:** Task 0.3 completed (Supabase project exists)

---

## Prerequisites

- [ ] Task 0.3 completed (Supabase project with DATABASE_URL)
- [ ] `.env.local` has `DATABASE_URL` set

---

## Entity Relationship Diagram

```
┌─────────────┐       ┌─────────────┐       ┌─────────────┐
│    User     │       │   Course    │       │   Chapter   │
├─────────────┤       ├─────────────┤       ├─────────────┤
│ id (PK)     │──┐    │ id (PK)     │──┐    │ id (PK)     │
│ email       │  │    │ title       │  │    │ courseId(FK)│──┐
│ name        │  │    │ description │  │    │ title       │  │
│ role        │  │    │ status      │  │    │ orderIndex  │  │
│ isActive    │  │    │ orderIndex  │  │    └─────────────┘  │
└─────────────┘  │    └─────────────┘  │                     │
      │          │          │          │                     │
      │          │          ▼          │                     │
      │          │    ┌─────────────┐  │    ┌─────────────┐  │
      │          │    │ Enrollment  │  │    │   Lesson    │  │
      │          │    ├─────────────┤  │    ├─────────────┤  │
      │          └───>│ userId (FK) │  │    │ id (PK)     │<─┘
      │               │ courseId(FK)│<─┘    │ chapterId(FK)
      │               │ completedAt │       │ title       │
      │               └─────────────┘       │ type        │
      │                                     │ status      │
      │                                     │ muxAssetId  │
      │          ┌──────────────────────────┤ resourceUrl │
      │          │                          └─────────────┘
      │          │                                │
      │          ▼                                │
      │    ┌─────────────┐                        │
      │    │LessonProgress                        │
      │    ├─────────────┤                        │
      └───>│ userId (FK) │                        │
           │ lessonId(FK)│<───────────────────────┘
           │ completed   │
           └─────────────┘

      ┌─────────────┐       ┌─────────────┐
      │QuizQuestion │       │ QuizAttempt │
      ├─────────────┤       ├─────────────┤
      │ id (PK)     │       │ id (PK)     │
      │ lessonId(FK)│       │ userId (FK) │
      │ questionText│       │ lessonId(FK)│
      │ type        │       │ score       │
      │ options     │       │ answers     │
      │ correctIndex│       └─────────────┘
      └─────────────┘

      ┌─────────────┐       ┌─────────────┐
      │    Note     │       │Announcement │
      ├─────────────┤       ├─────────────┤
      │ id (PK)     │       │ id (PK)     │
      │ userId (FK) │       │ title       │
      │ lessonId(FK)│       │ body        │
      │ content     │       │ status      │
      └─────────────┘       │ publishedAt │
                            └─────────────┘
```

---

## Complete Prisma Schema

Create `prisma/schema.prisma`:

```prisma
// prisma/schema.prisma

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider  = "postgresql"
  url       = env("DATABASE_URL")
  directUrl = env("DATABASE_URL")
}

// ============================================
// ENUMS
// ============================================

enum Role {
  ADMIN
  MEMBER
}

enum CourseStatus {
  DRAFT
  PUBLISHED
  ARCHIVED
}

enum LessonType {
  VIDEO
  QUIZ
  RESOURCE
}

enum LessonStatus {
  DRAFT
  PROCESSING
  READY
}

enum QuestionType {
  MULTIPLE_CHOICE
  TRUE_FALSE
}

enum AnnouncementStatus {
  DRAFT
  PUBLISHED
}

// ============================================
// USER MANAGEMENT
// ============================================

model User {
  id        String   @id @default(uuid())
  email     String   @unique
  name      String?
  avatarUrl String?  @map("avatar_url")
  role      Role     @default(MEMBER)
  isActive  Boolean  @default(true) @map("is_active")
  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @updatedAt @map("updated_at")

  // Relations
  enrollments    Enrollment[]
  lessonProgress LessonProgress[]
  quizAttempts   QuizAttempt[]
  notes          Note[]

  @@map("users")
}

// ============================================
// COURSE STRUCTURE
// ============================================

model Course {
  id           String       @id @default(uuid())
  title        String
  description  String?
  thumbnailUrl String?      @map("thumbnail_url")
  status       CourseStatus @default(DRAFT)
  orderIndex   Int          @default(0) @map("order_index")
  createdAt    DateTime     @default(now()) @map("created_at")
  updatedAt    DateTime     @updatedAt @map("updated_at")

  // Relations
  chapters    Chapter[]
  enrollments Enrollment[]

  @@map("courses")
}

model Chapter {
  id         String   @id @default(uuid())
  courseId   String   @map("course_id")
  title      String
  orderIndex Int      @default(0) @map("order_index")
  createdAt  DateTime @default(now()) @map("created_at")
  updatedAt  DateTime @updatedAt @map("updated_at")

  // Relations
  course  Course   @relation(fields: [courseId], references: [id], onDelete: Cascade)
  lessons Lesson[]

  @@map("chapters")
}

model Lesson {
  id          String       @id @default(uuid())
  chapterId   String       @map("chapter_id")
  title       String
  type        LessonType
  status      LessonStatus @default(DRAFT)
  orderIndex  Int          @default(0) @map("order_index")
  description String?

  // Video fields (type = VIDEO)
  muxAssetId      String? @map("mux_asset_id")
  muxPlaybackId   String? @map("mux_playback_id")
  durationSeconds Int?    @map("duration_seconds")

  // Resource fields (type = RESOURCE)
  resourceUrl  String? @map("resource_url")
  resourceName String? @map("resource_name")
  resourceSize Int?    @map("resource_size")

  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @updatedAt @map("updated_at")

  // Relations
  chapter        Chapter          @relation(fields: [chapterId], references: [id], onDelete: Cascade)
  quizQuestions  QuizQuestion[]
  lessonProgress LessonProgress[]
  quizAttempts   QuizAttempt[]
  notes          Note[]

  @@map("lessons")
}

// ============================================
// QUIZ SYSTEM
// ============================================

model QuizQuestion {
  id           String       @id @default(uuid())
  lessonId     String       @map("lesson_id")
  questionText String       @map("question_text")
  type         QuestionType
  options      Json         // ["Option A", "Option B", ...] or ["True", "False"]
  correctIndex Int          @map("correct_index") // Index of correct option
  orderIndex   Int          @default(0) @map("order_index")
  createdAt    DateTime     @default(now()) @map("created_at")
  updatedAt    DateTime     @updatedAt @map("updated_at")

  // Relations
  lesson Lesson @relation(fields: [lessonId], references: [id], onDelete: Cascade)

  @@map("quiz_questions")
}

model QuizAttempt {
  id        String   @id @default(uuid())
  userId    String   @map("user_id")
  lessonId  String   @map("lesson_id")
  score     Int      // Number correct
  total     Int      // Total questions
  answers   Json     // { questionId: selectedIndex, ... }
  createdAt DateTime @default(now()) @map("created_at")

  // Relations
  user   User   @relation(fields: [userId], references: [id], onDelete: Cascade)
  lesson Lesson @relation(fields: [lessonId], references: [id], onDelete: Cascade)

  @@map("quiz_attempts")
}

// ============================================
// USER PROGRESS
// ============================================

model Enrollment {
  id          String    @id @default(uuid())
  userId      String    @map("user_id")
  courseId    String    @map("course_id")
  enrolledAt  DateTime  @default(now()) @map("enrolled_at")
  completedAt DateTime? @map("completed_at")

  // Relations
  user   User   @relation(fields: [userId], references: [id], onDelete: Cascade)
  course Course @relation(fields: [courseId], references: [id], onDelete: Cascade)

  @@unique([userId, courseId])
  @@map("enrollments")
}

model LessonProgress {
  id          String    @id @default(uuid())
  userId      String    @map("user_id")
  lessonId    String    @map("lesson_id")
  completed   Boolean   @default(false)
  completedAt DateTime? @map("completed_at")
  createdAt   DateTime  @default(now()) @map("created_at")
  updatedAt   DateTime  @updatedAt @map("updated_at")

  // Relations
  user   User   @relation(fields: [userId], references: [id], onDelete: Cascade)
  lesson Lesson @relation(fields: [lessonId], references: [id], onDelete: Cascade)

  @@unique([userId, lessonId])
  @@map("lesson_progress")
}

// ============================================
// LESSON NOTES
// ============================================

model Note {
  id        String   @id @default(uuid())
  userId    String   @map("user_id")
  lessonId  String   @map("lesson_id")
  content   String   @default("")
  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @updatedAt @map("updated_at")

  // Relations
  user   User   @relation(fields: [userId], references: [id], onDelete: Cascade)
  lesson Lesson @relation(fields: [lessonId], references: [id], onDelete: Cascade)

  @@unique([userId, lessonId])
  @@map("notes")
}

// ============================================
// ANNOUNCEMENTS
// ============================================

model Announcement {
  id          String             @id @default(uuid())
  title       String
  body        String
  status      AnnouncementStatus @default(DRAFT)
  publishedAt DateTime?          @map("published_at")
  createdAt   DateTime           @default(now()) @map("created_at")
  updatedAt   DateTime           @updatedAt @map("updated_at")

  @@map("announcements")
}
```

---

## Table Summary

| Table | Purpose | Key Fields |
|-------|---------|------------|
| `users` | Member/Admin accounts | email, role, isActive |
| `courses` | Course containers | title, status, orderIndex |
| `chapters` | Sections within courses | title, orderIndex |
| `lessons` | Individual content items | type, status, muxAssetId |
| `quiz_questions` | Quiz questions | questionText, options, correctIndex |
| `quiz_attempts` | User quiz submissions | score, answers |
| `enrollments` | User-course relationship | enrolledAt, completedAt |
| `lesson_progress` | Lesson completion tracking | completed, completedAt |
| `notes` | Personal lesson notes | content |
| `announcements` | Admin broadcasts | title, body, status |

---

## Indexes (Created Automatically by Prisma)

Prisma creates indexes for:
- All `@id` fields
- All `@unique` fields and `@@unique` constraints
- All foreign key fields used in relations

Additional indexes will be added in RLS policies task.

---

## Relationships

| Parent | Child | Type | On Delete |
|--------|-------|------|-----------|
| User | Enrollment | 1:N | Cascade |
| User | LessonProgress | 1:N | Cascade |
| User | QuizAttempt | 1:N | Cascade |
| User | Note | 1:N | Cascade |
| Course | Chapter | 1:N | Cascade |
| Course | Enrollment | 1:N | Cascade |
| Chapter | Lesson | 1:N | Cascade |
| Lesson | QuizQuestion | 1:N | Cascade |
| Lesson | LessonProgress | 1:N | Cascade |
| Lesson | QuizAttempt | 1:N | Cascade |
| Lesson | Note | 1:N | Cascade |

---

## Files Created/Modified

| File | Action |
|------|--------|
| `prisma/schema.prisma` | Created with full schema |

---

## Verification

- [ ] `prisma/schema.prisma` exists with all tables
- [ ] All 10 models defined
- [ ] All enums defined (Role, CourseStatus, LessonType, LessonStatus, QuestionType, AnnouncementStatus)
- [ ] All relations have proper `onDelete: Cascade`
- [ ] Column names use snake_case via `@map`
- [ ] Table names use snake_case via `@@map`

---

## Schema Design Decisions

### Why snake_case for database?
PostgreSQL convention. Using `@map` and `@@map` allows camelCase in code.

### Why UUIDs instead of auto-increment?
- More secure (no enumeration attacks)
- Better for distributed systems
- Supabase convention

### Why cascade deletes?
- Deleting a course should delete all chapters, lessons, progress
- Simpler than manual cleanup
- Matches user expectation

### Why JSON for quiz options/answers?
- Flexible for varying number of options
- Simple to query in PostgreSQL
- No need for separate options table

---

## Next Task

→ **0.5-prisma-setup.md** (Prisma init + migrate)
