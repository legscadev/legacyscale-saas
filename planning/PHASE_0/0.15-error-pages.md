# Task 0.15 — Set Up Error Pages

## Overview

Create custom error pages for 404 (Not Found), 500 (Server Error), and other error states to provide a consistent user experience when things go wrong.

**Estimated Time:** 2 hours
**Priority:** P3 (Nice-to-have)
**Dependencies:** Task 0.2 completed (Tailwind + ShadCN configured)

---

## Prerequisites

- [ ] Task 0.2 completed (Tailwind + ShadCN configured)
- [ ] ShadCN button component installed
- [ ] Logo component created

---

## Error Pages Overview

| File | Status Code | Purpose |
|------|-------------|---------|
| `not-found.tsx` | 404 | Page not found |
| `error.tsx` | 500 | Server/runtime errors |
| `global-error.tsx` | 500 | Root layout errors |
| `unauthorized/page.tsx` | 403 | Access denied |
| `maintenance/page.tsx` | 503 | Maintenance mode |

---

## Steps

### Step 1: Create 404 Not Found Page

Create `app/not-found.tsx`:

```bash
cat > app/not-found.tsx << 'EOF'
import Link from 'next/link'
import { FileQuestion, Home, ArrowLeft } from 'lucide-react'
import { Button } from '@/components/ui/button'
import { Logo } from '@/components/layout/logo'

export default function NotFoundPage() {
  return (
    <div className="flex min-h-screen flex-col items-center justify-center bg-background p-4">
      <div className="w-full max-w-md text-center">
        {/* Logo */}
        <div className="mb-8 flex justify-center">
          <Logo />
        </div>

        {/* Icon */}
        <div className="mx-auto mb-6 flex h-20 w-20 items-center justify-center rounded-full bg-muted">
          <FileQuestion className="h-10 w-10 text-muted-foreground" />
        </div>

        {/* Content */}
        <h1 className="mb-2 text-4xl font-bold tracking-tight">404</h1>
        <h2 className="mb-4 text-xl font-semibold text-foreground">
          Page Not Found
        </h2>
        <p className="mb-8 text-muted-foreground">
          The page you&apos;re looking for doesn&apos;t exist or has been moved.
        </p>

        {/* Actions */}
        <div className="flex flex-col gap-3 sm:flex-row sm:justify-center">
          <Button asChild>
            <Link href="/dashboard">
              <Home className="mr-2 h-4 w-4" />
              Go to Dashboard
            </Link>
          </Button>
          <Button variant="outline" asChild>
            <Link href="javascript:history.back()">
              <ArrowLeft className="mr-2 h-4 w-4" />
              Go Back
            </Link>
          </Button>
        </div>
      </div>
    </div>
  )
}
EOF
```

### Step 2: Create Error Boundary Page

Create `app/error.tsx`:

```bash
cat > app/error.tsx << 'EOF'
'use client'

import { useEffect } from 'react'
import Link from 'next/link'
import { AlertTriangle, Home, RefreshCw } from 'lucide-react'
import { Button } from '@/components/ui/button'
import { Logo } from '@/components/layout/logo'

interface ErrorPageProps {
  error: Error & { digest?: string }
  reset: () => void
}

export default function ErrorPage({ error, reset }: ErrorPageProps) {
  useEffect(() => {
    // Log error to monitoring service
    console.error('Application error:', error)
  }, [error])

  return (
    <div className="flex min-h-screen flex-col items-center justify-center bg-background p-4">
      <div className="w-full max-w-md text-center">
        {/* Logo */}
        <div className="mb-8 flex justify-center">
          <Logo />
        </div>

        {/* Icon */}
        <div className="mx-auto mb-6 flex h-20 w-20 items-center justify-center rounded-full bg-destructive/10">
          <AlertTriangle className="h-10 w-10 text-destructive" />
        </div>

        {/* Content */}
        <h1 className="mb-2 text-4xl font-bold tracking-tight">Oops!</h1>
        <h2 className="mb-4 text-xl font-semibold text-foreground">
          Something Went Wrong
        </h2>
        <p className="mb-8 text-muted-foreground">
          An unexpected error occurred. Our team has been notified and is working on a fix.
        </p>

        {/* Error details (development only) */}
        {process.env.NODE_ENV === 'development' && (
          <div className="mb-8 rounded-lg bg-muted p-4 text-left">
            <p className="mb-2 text-sm font-medium text-destructive">
              Error Details:
            </p>
            <pre className="overflow-auto text-xs text-muted-foreground">
              {error.message}
            </pre>
            {error.digest && (
              <p className="mt-2 text-xs text-muted-foreground">
                Digest: {error.digest}
              </p>
            )}
          </div>
        )}

        {/* Actions */}
        <div className="flex flex-col gap-3 sm:flex-row sm:justify-center">
          <Button onClick={reset}>
            <RefreshCw className="mr-2 h-4 w-4" />
            Try Again
          </Button>
          <Button variant="outline" asChild>
            <Link href="/dashboard">
              <Home className="mr-2 h-4 w-4" />
              Go to Dashboard
            </Link>
          </Button>
        </div>
      </div>
    </div>
  )
}
EOF
```

### Step 3: Create Global Error Page

Create `app/global-error.tsx`:

```bash
cat > app/global-error.tsx << 'EOF'
'use client'

import { useEffect } from 'react'

interface GlobalErrorProps {
  error: Error & { digest?: string }
  reset: () => void
}

export default function GlobalError({ error, reset }: GlobalErrorProps) {
  useEffect(() => {
    console.error('Global error:', error)
  }, [error])

  return (
    <html>
      <body>
        <div
          style={{
            display: 'flex',
            minHeight: '100vh',
            flexDirection: 'column',
            alignItems: 'center',
            justifyContent: 'center',
            backgroundColor: '#09090b',
            padding: '1rem',
            color: '#fafafa',
            fontFamily: 'system-ui, sans-serif',
          }}
        >
          <div style={{ maxWidth: '28rem', textAlign: 'center' }}>
            {/* Logo placeholder */}
            <div
              style={{
                marginBottom: '2rem',
                fontSize: '1.5rem',
                fontWeight: 'bold',
                color: '#8B5CF6',
              }}
            >
              Legacy Scale
            </div>

            {/* Icon */}
            <div
              style={{
                marginBottom: '1.5rem',
                fontSize: '4rem',
              }}
            >
              ⚠️
            </div>

            {/* Content */}
            <h1
              style={{
                marginBottom: '0.5rem',
                fontSize: '2rem',
                fontWeight: 'bold',
              }}
            >
              Critical Error
            </h1>
            <p
              style={{
                marginBottom: '2rem',
                color: '#a1a1aa',
              }}
            >
              A critical error occurred. Please try refreshing the page.
            </p>

            {/* Action */}
            <button
              onClick={reset}
              style={{
                padding: '0.75rem 1.5rem',
                backgroundColor: '#8B5CF6',
                color: '#ffffff',
                border: 'none',
                borderRadius: '0.5rem',
                fontSize: '1rem',
                fontWeight: 'bold',
                cursor: 'pointer',
              }}
            >
              Try Again
            </button>
          </div>
        </div>
      </body>
    </html>
  )
}
EOF
```

### Step 4: Create Unauthorized Page

Create `app/unauthorized/page.tsx`:

```bash
mkdir -p app/unauthorized

cat > app/unauthorized/page.tsx << 'EOF'
import Link from 'next/link'
import { ShieldX, Home, LogIn } from 'lucide-react'
import { Button } from '@/components/ui/button'
import { Logo } from '@/components/layout/logo'

export default function UnauthorizedPage() {
  return (
    <div className="flex min-h-screen flex-col items-center justify-center bg-background p-4">
      <div className="w-full max-w-md text-center">
        {/* Logo */}
        <div className="mb-8 flex justify-center">
          <Logo />
        </div>

        {/* Icon */}
        <div className="mx-auto mb-6 flex h-20 w-20 items-center justify-center rounded-full bg-destructive/10">
          <ShieldX className="h-10 w-10 text-destructive" />
        </div>

        {/* Content */}
        <h1 className="mb-2 text-4xl font-bold tracking-tight">403</h1>
        <h2 className="mb-4 text-xl font-semibold text-foreground">
          Access Denied
        </h2>
        <p className="mb-8 text-muted-foreground">
          You don&apos;t have permission to access this page. Please contact an administrator if you believe this is an error.
        </p>

        {/* Actions */}
        <div className="flex flex-col gap-3 sm:flex-row sm:justify-center">
          <Button asChild>
            <Link href="/dashboard">
              <Home className="mr-2 h-4 w-4" />
              Go to Dashboard
            </Link>
          </Button>
          <Button variant="outline" asChild>
            <Link href="/login">
              <LogIn className="mr-2 h-4 w-4" />
              Sign In
            </Link>
          </Button>
        </div>
      </div>
    </div>
  )
}
EOF
```

### Step 5: Create Maintenance Page

Create `app/maintenance/page.tsx`:

```bash
mkdir -p app/maintenance

cat > app/maintenance/page.tsx << 'EOF'
import { Wrench, Clock } from 'lucide-react'
import { Logo } from '@/components/layout/logo'

export default function MaintenancePage() {
  return (
    <div className="flex min-h-screen flex-col items-center justify-center bg-background p-4">
      <div className="w-full max-w-md text-center">
        {/* Logo */}
        <div className="mb-8 flex justify-center">
          <Logo />
        </div>

        {/* Icon */}
        <div className="mx-auto mb-6 flex h-20 w-20 items-center justify-center rounded-full bg-warning/10">
          <Wrench className="h-10 w-10 text-warning" />
        </div>

        {/* Content */}
        <h1 className="mb-2 text-2xl font-bold tracking-tight">
          Under Maintenance
        </h1>
        <p className="mb-8 text-muted-foreground">
          We&apos;re performing scheduled maintenance to improve your experience. We&apos;ll be back shortly!
        </p>

        {/* Estimated time */}
        <div className="inline-flex items-center gap-2 rounded-full bg-muted px-4 py-2 text-sm text-muted-foreground">
          <Clock className="h-4 w-4" />
          <span>Estimated downtime: 30 minutes</span>
        </div>

        {/* Status updates */}
        <p className="mt-8 text-sm text-muted-foreground">
          Follow{' '}
          <a
            href="https://twitter.com/legacyscale"
            target="_blank"
            rel="noopener noreferrer"
            className="text-primary hover:underline"
          >
            @legacyscale
          </a>
          {' '}for updates.
        </p>
      </div>
    </div>
  )
}
EOF
```

### Step 6: Create Loading Component

Create `app/loading.tsx`:

```bash
cat > app/loading.tsx << 'EOF'
import { Loader2 } from 'lucide-react'

export default function Loading() {
  return (
    <div className="flex min-h-screen items-center justify-center">
      <div className="flex flex-col items-center gap-4">
        <Loader2 className="h-8 w-8 animate-spin text-primary" />
        <p className="text-sm text-muted-foreground">Loading...</p>
      </div>
    </div>
  )
}
EOF
```

### Step 7: Create Route-Specific Loading States

Create admin loading:

```bash
cat > "app/(admin)/admin/loading.tsx" << 'EOF'
import { PageLoadingSkeleton } from '@/components/shared'

export default function AdminLoading() {
  return <PageLoadingSkeleton />
}
EOF
```

Create user loading:

```bash
cat > "app/(user)/loading.tsx" << 'EOF'
import { PageLoadingSkeleton } from '@/components/shared'

export default function UserLoading() {
  return <PageLoadingSkeleton />
}
EOF
```

### Step 8: Create Specific Route Not Found Pages

Create admin not found:

```bash
cat > "app/(admin)/admin/not-found.tsx" << 'EOF'
import Link from 'next/link'
import { FileQuestion } from 'lucide-react'
import { Button } from '@/components/ui/button'
import { EmptyState } from '@/components/shared'

export default function AdminNotFound() {
  return (
    <div className="flex items-center justify-center min-h-[60vh]">
      <EmptyState
        icon={FileQuestion}
        title="Page Not Found"
        description="The admin page you're looking for doesn't exist."
      >
        <Button asChild>
          <Link href="/admin">Back to Admin Dashboard</Link>
        </Button>
      </EmptyState>
    </div>
  )
}
EOF
```

Create user not found:

```bash
cat > "app/(user)/not-found.tsx" << 'EOF'
import Link from 'next/link'
import { FileQuestion } from 'lucide-react'
import { Button } from '@/components/ui/button'
import { EmptyState } from '@/components/shared'

export default function UserNotFound() {
  return (
    <div className="flex items-center justify-center min-h-[60vh]">
      <EmptyState
        icon={FileQuestion}
        title="Page Not Found"
        description="The page you're looking for doesn't exist."
      >
        <Button asChild>
          <Link href="/dashboard">Back to Dashboard</Link>
        </Button>
      </EmptyState>
    </div>
  )
}
EOF
```

### Step 9: Create Route-Specific Error Pages

Create admin error:

```bash
cat > "app/(admin)/admin/error.tsx" << 'EOF'
'use client'

import { useEffect } from 'react'
import { AlertTriangle, RefreshCw } from 'lucide-react'
import { Button } from '@/components/ui/button'
import { EmptyState } from '@/components/shared'

interface AdminErrorProps {
  error: Error & { digest?: string }
  reset: () => void
}

export default function AdminError({ error, reset }: AdminErrorProps) {
  useEffect(() => {
    console.error('Admin error:', error)
  }, [error])

  return (
    <div className="flex items-center justify-center min-h-[60vh]">
      <EmptyState
        icon={AlertTriangle}
        title="Something Went Wrong"
        description="An error occurred in the admin section. Please try again."
      >
        <Button onClick={reset}>
          <RefreshCw className="mr-2 h-4 w-4" />
          Try Again
        </Button>
      </EmptyState>
    </div>
  )
}
EOF
```

Create user error:

```bash
cat > "app/(user)/error.tsx" << 'EOF'
'use client'

import { useEffect } from 'react'
import { AlertTriangle, RefreshCw } from 'lucide-react'
import { Button } from '@/components/ui/button'
import { EmptyState } from '@/components/shared'

interface UserErrorProps {
  error: Error & { digest?: string }
  reset: () => void
}

export default function UserError({ error, reset }: UserErrorProps) {
  useEffect(() => {
    console.error('User error:', error)
  }, [error])

  return (
    <div className="flex items-center justify-center min-h-[60vh]">
      <EmptyState
        icon={AlertTriangle}
        title="Something Went Wrong"
        description="An error occurred. Please try again."
      >
        <Button onClick={reset}>
          <RefreshCw className="mr-2 h-4 w-4" />
          Try Again
        </Button>
      </EmptyState>
    </div>
  )
}
EOF
```

---

## Files Created/Modified

| File | Action |
|------|--------|
| `app/not-found.tsx` | Created |
| `app/error.tsx` | Created |
| `app/global-error.tsx` | Created |
| `app/loading.tsx` | Created |
| `app/unauthorized/page.tsx` | Created |
| `app/maintenance/page.tsx` | Created |
| `app/(admin)/admin/loading.tsx` | Created |
| `app/(admin)/admin/not-found.tsx` | Created |
| `app/(admin)/admin/error.tsx` | Created |
| `app/(user)/loading.tsx` | Created |
| `app/(user)/not-found.tsx` | Created |
| `app/(user)/error.tsx` | Created |

---

## Verification

- [ ] 404 page renders for non-existent routes
- [ ] Error page renders when errors occur
- [ ] Global error page works as fallback
- [ ] Loading states show during navigation
- [ ] Unauthorized page accessible at `/unauthorized`
- [ ] Maintenance page accessible at `/maintenance`
- [ ] Route-specific error pages work within layouts
- [ ] All pages have consistent branding

---

## Error Pages Summary

| Page | Purpose | When Shown |
|------|---------|------------|
| `not-found.tsx` | 404 errors | Invalid routes |
| `error.tsx` | Runtime errors | Uncaught exceptions |
| `global-error.tsx` | Root errors | Layout crashes |
| `loading.tsx` | Loading state | During navigation |
| `unauthorized` | 403 errors | Access denied |
| `maintenance` | 503 status | Planned downtime |

---

## Testing Error Pages

### Test 404

Visit any non-existent route:
```
http://localhost:3000/this-page-does-not-exist
```

### Test Error Boundary

Create a test component that throws:
```tsx
// app/test-error/page.tsx
'use client'

export default function TestErrorPage() {
  throw new Error('Test error!')
}
```

### Test Loading

Add artificial delay to a page:
```tsx
// In any server component
await new Promise(resolve => setTimeout(resolve, 3000))
```

---

## Error Monitoring (Future)

Consider adding error monitoring services:
- **Sentry**: Full error tracking and monitoring
- **LogRocket**: Session replay and logging
- **Vercel Analytics**: Built-in error tracking

```typescript
// Example Sentry integration in error.tsx
useEffect(() => {
  Sentry.captureException(error)
}, [error])
```

---

## Next Task

→ **0.16-zod-validation.md** (Configure Zod for API validation)
